<!DOCTYPE html>
<html>
  <head>
    <title><%= @song.name %></title>
  </head>
  <body>
  	<h1><%= @song.name %></h1><br>
  	<a href="/artists/<%= @song.artist.slug %>"> <%= @song.artist.name %></a>
  	<br>
  	<a href=<%= "/genres/#{@song.genres[0].slug}" %>><%= @song.genres[0].name %></a>
  	<br>
  </body>
</html>
